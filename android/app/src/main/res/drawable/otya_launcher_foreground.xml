<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- One reviewed high-resolution transparent mark is scaled by the
         adaptive-icon layer. Android still owns the final launcher mask. -->
    <item>
        <bitmap
            android:src="@drawable/otya_launcher_foreground_bitmap"
            android:gravity="fill" />
    </item>
</layer-list>