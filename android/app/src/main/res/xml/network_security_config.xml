<?xml version="1.0" encoding="utf-8"?>
<!--
  OTYA uses short-lived HTTP only for local-device features such as Beam and
  Web Mirror. Android Network Security Config cannot express RFC1918 wildcard
  IP ranges (for example every 192.168.x.x address), so dynamic phone/hotspot
  addresses require cleartext at the base policy. OTYA's internet endpoints are
  hard-coded as HTTPS and the PeterSmart Link domain is explicitly HTTPS-only.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">petersmartlink.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
