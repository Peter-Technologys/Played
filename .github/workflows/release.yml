# ───────────────────────────────────────────────────────────────
# OTYA Player — GitHub Actions Release Pipeline
#
# Triggers on v* tags pushed from GitLab mirror (e.g. v1.3.0).
# Builds signed split APKs, uploads to Cloudflare R2, notifies Appwrite.
#
# Required GitHub Secrets (Settings → Secrets and variables → Actions → Secrets):
#   KEYSTORE_BASE64        — base64-encoded .jks keystore
#   STORE_PASSWORD         — keystore password
#   KEY_ALIAS              — key alias
#   KEY_PASSWORD           — key password
#   R2_ACCESS_KEY_ID       — Cloudflare R2 Access Key ID
#   R2_SECRET_ACCESS_KEY   — Cloudflare R2 Secret Access Key
#   APPWRITE_API_KEY       — Appwrite Server API Key
#
# Required GitHub Variables (Settings → Secrets and variables → Actions → Variables):
#   GOOGLE_SERVICES_JSON   — raw contents of google-services.json
#   R2_ENDPOINT            — https://6c76d2a9e5f95d5f9b393e97374d9afb.r2.cloudflarestorage.com
#   R2_BUCKET              — otya-player-releases
#   APPWRITE_ENDPOINT      — https://nyc.cloud.appwrite.io/v1
#   APPWRITE_PROJECT_ID    — your Appwrite project ID
#   WORKER_URL             — https://getotya.petersmartlink.com
# ───────────────────────────────────────────────────────────────

name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Version tag to release (e.g. v1.3.0)'
        required: true

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

env:
  PUB_ENVIRONMENT: bot.github
  GRADLE_OPTS: "-Dorg.gradle.daemon=false -Dorg.gradle.parallel=true -Xmx4g"

jobs:
  build_and_publish:
    name: Build Signed Release
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # ── Free disk space ────────────────────────────────────────────────
      - name: Free disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: false
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x'
          channel: stable
          cache: true

      - uses: actions/cache@v4
        with:
          path: ~/.pub-cache
          key: pub-${{ hashFiles('pubspec.yaml') }}
          restore-keys: pub-

      - name: Install AWS CLI
        run: pip install awscli --quiet

      - run: flutter pub get

      # ── Inject secrets ─────────────────────────────────────────────────
      - name: Write google-services.json
        run: echo '${{ vars.GOOGLE_SERVICES_JSON }}' > android/app/google-services.json

      - name: Decode keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > android/app/keystore.jks

      # ── Build ──────────────────────────────────────────────────────────
      - name: Build release AAB
        env:
          KEYSTORE_PATH: keystore.jks
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          flutter build appbundle --release \
            --obfuscate \
            --split-debug-info=build/debug-info

      - name: Build split APKs (arm64 + arm32)
        env:
          KEYSTORE_PATH: keystore.jks
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          flutter build apk --release --split-per-abi \
            --target-platform android-arm,android-arm64 \
            --obfuscate \
            --split-debug-info=build/debug-info

      # ── Verify APK files exist before uploading ─────────────────────────
      - name: Verify APK output files
        run: |
          echo "=== APK output directory ==="
          ls -lh build/app/outputs/flutter-apk/
          # Fail fast if expected files are missing
          test -f build/app/outputs/flutter-apk/app-arm64-v8a-release.apk || \
          test -f build/app/outputs/flutter-apk/app-arm64-v8a-standard-release.apk || \
          { echo "ERROR: arm64 APK not found"; exit 1; }

      - name: Remove secrets
        if: always()
        run: |
          rm -f android/app/keystore.jks
          rm -f android/app/google-services.json

      # ── Resolve actual APK filenames (signing config name varies) ──────
      - name: Resolve APK paths
        id: apk
        run: |
          APK_DIR=build/app/outputs/flutter-apk
          # Flutter names the APK based on the signing config name.
          # Try 'standard' first (most common), then fall back to no-suffix.
          if [ -f "$APK_DIR/app-arm64-v8a-standard-release.apk" ]; then
            echo "arm64=$APK_DIR/app-arm64-v8a-standard-release.apk" >> $GITHUB_OUTPUT
            echo "arm32=$APK_DIR/app-armeabi-v7a-standard-release.apk" >> $GITHUB_OUTPUT
          else
            echo "arm64=$APK_DIR/app-arm64-v8a-release.apk" >> $GITHUB_OUTPUT
            echo "arm32=$APK_DIR/app-armeabi-v7a-release.apk" >> $GITHUB_OUTPUT
          fi

      # ── Publish to Cloudflare R2 ────────────────────────────────────────
      - name: Publish to Cloudflare R2
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: auto
          R2_ENDPOINT: ${{ vars.R2_ENDPOINT }}
          R2_BUCKET: ${{ vars.R2_BUCKET }}
          WORKER_URL: ${{ vars.WORKER_URL }}
          APPWRITE_ENDPOINT: ${{ vars.APPWRITE_ENDPOINT }}
          APPWRITE_PROJECT_ID: ${{ vars.APPWRITE_PROJECT_ID }}
          APPWRITE_API_KEY: ${{ secrets.APPWRITE_API_KEY }}
          ARM64_APK: ${{ steps.apk.outputs.arm64 }}
          ARM32_APK: ${{ steps.apk.outputs.arm32 }}
        run: |
          set -euo pipefail

          VERSION=${GITHUB_REF_NAME#v}
          VERSION_CODE=$(echo "$VERSION" | awk -F. '{printf "%d%02d%02d", $1, $2, $3}')
          echo "Publishing OTYA Player v$VERSION (code $VERSION_CODE)"

          # Read changelog from CHANGELOG.md top section
          CHANGELOG=$(awk '/^## \[/{found++} found==1{print} found==2{exit}' CHANGELOG.md \
            | tail -n +2 | head -40 | tr '\n' ' ' \
            | sed 's/  */ /g; s/^[[:space:]]*//; s/[[:space:]]*$//')
          [ -z "$CHANGELOG" ] && CHANGELOG="Bug fixes and improvements"

          # Read SDK versions from build.gradle
          MIN_SDK=$(grep 'minSdk' android/app/build.gradle | grep -v '//' | head -1 | grep -oP '\d+' || echo 21)
          TARGET_SDK=$(grep 'targetSdk' android/app/build.gradle | grep -v '//' | head -1 | grep -oP '\d+' || echo 36)

          # Backup current version before overwriting
          CURRENT_VERSION=$(aws s3 cp \
            s3://$R2_BUCKET/version.json - \
            --endpoint-url $R2_ENDPOINT 2>/dev/null \
            | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('version',''))" 2>/dev/null || echo "")

          if [ -n "$CURRENT_VERSION" ] && [ "$CURRENT_VERSION" != "$VERSION" ]; then
            echo "Backing up v$CURRENT_VERSION..."
            for KEY in app-arm64-v8a-standard-release.apk app-armeabi-v7a-standard-release.apk version.json; do
              aws s3 cp \
                s3://$R2_BUCKET/$KEY \
                s3://$R2_BUCKET/releases/v$CURRENT_VERSION/$KEY \
                --endpoint-url $R2_ENDPOINT 2>/dev/null || true
            done
          fi

          # Upload APKs with consistent names (Worker always reads these exact names)
          aws s3 cp \
            "$ARM64_APK" \
            s3://$R2_BUCKET/app-arm64-v8a-standard-release.apk \
            --endpoint-url $R2_ENDPOINT \
            --content-type application/vnd.android.package-archive \
            --cache-control "public, max-age=31536000, immutable"

          aws s3 cp \
            "$ARM32_APK" \
            s3://$R2_BUCKET/app-armeabi-v7a-standard-release.apk \
            --endpoint-url $R2_ENDPOINT \
            --content-type application/vnd.android.package-archive \
            --cache-control "public, max-age=31536000, immutable"

          # Generate version.json
          DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          python3 - <<PYEOF
          import json
          data = {
              "version":     "$VERSION",
              "versionCode": int("$VERSION_CODE"),
              "date":        "$DATE",
              "arm64":       "app-arm64-v8a-standard-release.apk",
              "arm32":       "app-armeabi-v7a-standard-release.apk",
              "changelog":   """$CHANGELOG""",
              "minSdk":      int("$MIN_SDK"),
              "targetSdk":   int("$TARGET_SDK"),
              "workerUrl":   "$WORKER_URL",
              "downloads": {
                  "arm64": "$WORKER_URL/apk/arm64",
                  "arm32": "$WORKER_URL/apk/arm32",
                  "auto":  "$WORKER_URL/download"
              }
          }
          with open('version.json', 'w') as f:
              json.dump(data, f, indent=2)
              f.write('\n')
          print(json.dumps(data, indent=2))
          PYEOF

          python3 -c "import json; json.load(open('version.json'))" || { echo "ERROR: invalid version.json"; exit 1; }

          # Upload version.json LAST so Worker is always consistent
          aws s3 cp \
            version.json \
            s3://$R2_BUCKET/version.json \
            --endpoint-url $R2_ENDPOINT \
            --content-type application/json \
            --cache-control "public, max-age=300"

          echo "✓ R2 upload complete."

          # Notify Appwrite (creates release document for in-app update checker)
          if [ -n "${APPWRITE_ENDPOINT:-}" ] && [ -n "${APPWRITE_PROJECT_ID:-}" ] && [ -n "${APPWRITE_API_KEY:-}" ]; then
            DOC_ID=$(echo "v$VERSION" | tr '.' '-')
            DOC_BODY=$(python3 -c "
import json
print(json.dumps({
    'documentId': '$DOC_ID',
    'data': {
        'version':     '$VERSION',
        'versionCode': int('$VERSION_CODE'),
        'date':        '$DATE',
        'changelog':   '$CHANGELOG',
        'arm64Url':    '$WORKER_URL/apk/arm64',
        'arm32Url':    '$WORKER_URL/apk/arm32',
        'downloadUrl': '$WORKER_URL/download',
        'minSdk':      int('$MIN_SDK'),
        'targetSdk':   int('$TARGET_SDK')
    }
}))
")
            HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
              -X POST \
              "${APPWRITE_ENDPOINT}/databases/otya-db/collections/releases/documents" \
              -H "Content-Type: application/json" \
              -H "X-Appwrite-Project: ${APPWRITE_PROJECT_ID}" \
              -H "X-Appwrite-Key: ${APPWRITE_API_KEY}" \
              -d "$DOC_BODY")
            echo "Appwrite response: $HTTP_STATUS"
          else
            echo "Appwrite vars not set — skipping notification."
          fi

          # Prune old backups (keep last 5)
          VERSIONS=$(aws s3 ls s3://$R2_BUCKET/releases/ \
            --endpoint-url $R2_ENDPOINT 2>/dev/null \
            | awk '{print $2}' | sort -V | head -n -5)
          for OLD in $VERSIONS; do
            aws s3 rm s3://$R2_BUCKET/releases/$OLD \
              --endpoint-url $R2_ENDPOINT --recursive 2>/dev/null || true
          done

          echo "✓ Publish complete. Live at $WORKER_URL"

      # ── GitHub Release ─────────────────────────────────────────────────
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            ${{ steps.apk.outputs.arm64 }}
            ${{ steps.apk.outputs.arm32 }}
            build/app/outputs/bundle/release/app-release.aab
            version.json

      # ── Upload artifacts ───────────────────────────────────────────────
      - uses: actions/upload-artifact@v4
        with:
          name: release-apks-${{ github.ref_name }}
          path: |
            ${{ steps.apk.outputs.arm64 }}
            ${{ steps.apk.outputs.arm32 }}
          retention-days: 30

      - uses: actions/upload-artifact@v4
        with:
          name: debug-info-${{ github.ref_name }}
          path: build/debug-info/
          retention-days: 30
