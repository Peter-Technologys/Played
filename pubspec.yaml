  # Appwrite — cloud sync, auth, backup
  # Pin to 12.x which uses flutter_web_auth_2 ^3.0.0 (Kotlin 2.x compatible)
  appwrite: ^12.0.1

  # Google Drive backup
  googleapis: ^12.0.0
  googleapis_auth: ^1.6.0
  google_sign_in: ^6.2.1